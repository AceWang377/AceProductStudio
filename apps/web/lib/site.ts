export const siteConfig = {
  name: "AI Product Studio",
  company: process.env.NEXT_PUBLIC_COMPANY_NAME?.trim() || "AI Product Studio",
  supportEmail: process.env.NEXT_PUBLIC_SUPPORT_EMAIL?.trim() || "support@aceproductstudio.com"
};
